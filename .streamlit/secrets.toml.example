# Copia este archivo como  secrets.toml  y pon tu clave real.
# NUNCA subas secrets.toml a GitHub (ya esta en .gitignore).
# En Streamlit Cloud esto se configura en el panel "Secrets".

GEMINI_API_KEY = "tu_clave_de_google_gemini_aqui"
